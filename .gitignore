# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
files.zip
dist/
pre-shadcn-backup.zip

# Secrets
public/config.env.php
.env
.env.*
!.env.example

# Agent / AI tooling (per-developer)
.claude/worktrees/
.claude/skills/
.agents/
.kiro/
skills/
skills-lock.json
file_tree.txt
CLAUDE.md

# Local stash + journals (not for repo)
stash/
diary/

# Heavy media never goes in repo
*.mp4
*.mov
*.avi
*.mkv

# Client offer data — NEVER commits (RODO + business sensitivity)
# Kod ofert (komponenty, route, gate) jest publiczny.
# Treść konkretnych klientów + ich dane osobowe — gitignored.
src/data/offers/*.js
!src/data/offers/index.js
!src/data/offers/*.example.js
api/_data/offers/*.js
api/_data/offers/_views.json
!api/_data/offers/*.example.js
public/offers/

# Generated PDF exports of offers (mogą zawierać dane klientów)
offers-pdf/

# Wewnętrzne notatki strategiczne — NIE do publicznego repo.
# Jeśli chcesz przenieść do repo, usuń z tej listy i sprawdź czy nie zawierają
# wrażliwych nazwisk / cen / strategii konkurencyjnej.
SALES_PLAN.md
TOOLS_DEEP_DIVE.md
.vercel
